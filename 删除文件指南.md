# ✅ 完整清理方案

## 📍 确认：这4个文件还在你的代码里

```
pages/api/webhooks/app/uninstalled.ts                    ✓ 存在
pages/api/webhooks/privacy/customers-data-request.ts     ✓ 存在
pages/api/webhooks/privacy/customers-redact.ts           ✓ 存在
pages/api/webhooks/privacy/shop-redact.ts                ✓ 存在
```

它们都引用了已删除的 `lib/shopify-auth.ts`，所以编译失败。

---

## 🚀 方法1：一键删除（推荐）

双击运行：
```
delete-broken-files.bat
```

然后提交：
```bash
git add -A
git commit -m "fix: 删除损坏的webhook文件"
git push origin main --force
```

---

## 📝 方法2：手动删除

### 在文件管理器或VS Code中删除这4个文件：

1. `pages/api/webhooks/app/uninstalled.ts`
2. `pages/api/webhooks/privacy/customers-data-request.ts`
3. `pages/api/webhooks/privacy/customers-redact.ts`
4. `pages/api/webhooks/privacy/shop-redact.ts`

### 然后提交：
```bash
git add -A
git commit -m "fix: 删除损坏的webhook文件"
git push origin main --force
```

---

## ✅ 验证

删除后运行：
```bash
git status
```

应该显示：
```
deleted: pages/api/webhooks/app/uninstalled.ts
deleted: pages/api/webhooks/privacy/customers-data-request.ts
deleted: pages/api/webhooks/privacy/customers-redact.ts
deleted: pages/api/webhooks/privacy/shop-redact.ts
```

---

**删除这4个文件后，Vercel部署就会成功！** 🎉
