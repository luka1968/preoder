{% comment %}
  PreOrder Pro - App Embed Block
  这个文件会被自动注入到 theme.liquid 的 </head> 标签前
{% endcomment %}

<script>
  // PreOrder Pro 配置
  window.PREORDER_CONFIG = {
    shop: '{{ shop.permanent_domain }}',
    apiUrl: 'https://shopmall.dpdns.org/api',
    enabled: true,
    debug: true,
    version: '1.0.0'
  };
  
  console.log('🚀 PreOrder Pro App Embed Block Loaded');
  console.log('📊 Config:', window.PREORDER_CONFIG);
</script>

<!-- 加载通用预购脚本 -->
<script>
(function() {
  'use strict';
  
  // 防止重复加载
  if (window.PreOrderProLoaded) {
    console.log('✅ PreOrder Pro already loaded');
    return;
  }
  
  window.PreOrderProLoaded = true;
  
  // 动态加载预购脚本
  const script = document.createElement('script');
  script.src = 'https://shopmall.dpdns.org/universal-preorder.js';
  script.async = true;
  script.onload = function() {
    console.log('✅ PreOrder Universal Widget loaded via App Embed');
    
    // 触发自定义事件，通知脚本已加载
    if (window.dispatchEvent) {
      window.dispatchEvent(new CustomEvent('preorder:loaded', {
        detail: { source: 'app-embed', config: window.PREORDER_CONFIG }
      }));
    }
  };
  script.onerror = function() {
    console.error('❌ Failed to load PreOrder Universal Widget');
  };
  
  document.head.appendChild(script);
})();
</script>

<!-- 预购样式 -->
<style>
/* PreOrder Pro - 通用样式 */
.preorder-btn {
  background: linear-gradient(135deg, #ff6b35 0%, #f7931e 100%) !important;
  color: white !important;
  border: none !important;
  padding: 15px 30px !important;
  border-radius: 8px !important;
  font-weight: bold !important;
  cursor: pointer !important;
  width: 100% !important;
  font-size: 16px !important;
  margin: 10px 0 !important;
  transition: all 0.3s ease !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  box-shadow: 0 4px 12px rgba(255, 107, 53, 0.3) !important;
  text-transform: none !important;
  letter-spacing: normal !important;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif !important;
}

.preorder-btn:hover {
  background: linear-gradient(135deg, #e55a2b 0%, #d7831a 100%) !important;
  transform: translateY(-2px) !important;
  box-shadow: 0 6px 20px rgba(255, 107, 53, 0.4) !important;
}
</style>

{% schema %}
{
  "name": "PreOrder Pro",
  "target": "head",
  "settings": [
    {
      "type": "checkbox",
      "id": "enabled",
      "label": "启用预购功能",
      "default": true
    },
    {
      "type": "text",
      "id": "api_url",
      "label": "API URL",
      "default": "https://shopmall.dpdns.org/api"
    },
    {
      "type": "checkbox",
      "id": "debug",
      "label": "调试模式",
      "default": false
    }
  ]
}
{% endschema %}
